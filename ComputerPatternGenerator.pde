void setup(){
size(700,550);
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke(x+y%x-y-x-x/y/x-y/x+y-x*y/y-y*y%x);
point(x,y);
}
}
saveFrame("pattern1.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y/y%y/y+x*x-y+x%y)%255);
point(x,y);
}
}
saveFrame("pattern2.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y%x*y/x/y*x-x)%255);
point(x,y);
}
}
saveFrame("pattern3.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y%y%x*x-x+x%x/x)%255);
point(x,y);
}
}
saveFrame("pattern4.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y*x*y/y%x-y*y-x%y%y*x*y)%255);
point(x,y);
}
}
saveFrame("pattern5.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y+x-y+y*y/x+y/y+x-x+x%y%x-x%x/x+y+y%y)%255);
point(x,y);
}
}
saveFrame("pattern6.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x*x%y+y+x/x*y-x/y+y-x*x%x+x*y+x%y+y*x)%255);
point(x,y);
}
}
saveFrame("pattern7.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x-y+y-y-y+x*x%x/x*x/y/x-y+x%x+y+x/y-x%y*x)%255);
point(x,y);
}
}
saveFrame("pattern8.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x*x/y/x+y/x%y-y%x+y*y-y%y)%255);
point(x,y);
}
}
saveFrame("pattern9.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y*y+y*x-x-y-y*y*y+x*x-x+y+x-x-y*y/y*x%x*y)%255);
point(x,y);
}
}
saveFrame("pattern10.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y%y/x+y/y-y*y+y)%255);
point(x,y);
}
}
saveFrame("pattern11.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke(y*x%x*y/x*x+x%y*y%y+y-x*y/x%y-x);
point(x,y);
}
}
saveFrame("pattern12.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x+x*x+x/x+x/x)%255);
point(x,y);
}
}
saveFrame("pattern13.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x-y+y+y+y+x*x)%255);
point(x,y);
}
}
saveFrame("pattern14.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x*x+x-x/x%x)%255);
point(x,y);
}
}
saveFrame("pattern15.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke(x-x%x%y-y*y%x/x-x-x+y*y/y%x*x/x*x/x%x%x+x);
point(x,y);
}
}
saveFrame("pattern16.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x-x*y*y+y+x*y/x/y+y%x*y-y+x%y*x+x/x/y-y-y)%255);
point(x,y);
}
}
saveFrame("pattern17.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y+x%x*x-x+x*x)%255);
point(x,y);
}
}
saveFrame("pattern18.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((x%x*x/x*y%x)%255);
point(x,y);
}
}
saveFrame("pattern19.png");
println("Finished");
for(int x = 1; x < width; x ++){
for(int y = 1; y < height; y ++){
stroke((y/y*x*y-x*y%y/y+x-y+x/x*x*x+x*y)%255);
point(x,y);
}
}
saveFrame("pattern20.png");
}
